#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int N = atoi(argv);
    int *a = malloc(N*sizeof(int));
    int i, j, k;

    for (i = 0; i < N; i++)
        scanf("%d", )
}